import { useState } from 'react'
import { sendFeedback } from '../lib/api'
export default function Feedback(){const [msg,setMsg]=useState('');const [ok,setOk]=useState('');const [err,setErr]=useState('');async function submit(){setErr('');setOk('');try{const r=await sendFeedback({targetType:'GENERAL',message:msg,consentToContact:false});setOk(`Odesláno: ${r.feedbackId}`);setMsg('')}catch(e:any){setErr(String(e.message||e))}}return (<div className='mx-auto max-w-3xl px-4 py-6 space-y-3'><h1 className='text-2xl font-semibold'>Připomínka</h1><textarea className='w-full rounded-2xl border bg-white p-3 text-sm min-h-36' value={msg} onChange={e=>setMsg(e.target.value)} placeholder='Napiš zprávu…'/><button className='rounded-xl bg-black text-white px-4 py-2 text-sm' onClick={submit} disabled={msg.trim().length<10}>Odeslat</button>{ok&&<div className='text-sm text-green-700'>{ok}</div>}{err&&<div className='text-sm text-red-700'>{err}</div>}</div>)}
